#include <stdio.h>
int is_prime(int n)
{
	int z = 0;
	for (z = 2; z < n; z++)
	{
		if (n % z == 0)
		{
			return 0;
		}
		return 1;
	}
	
}
int main()
{
	int i = 0;
	for (i = 100; i <= 200; i++)
	{
		if (is_prime(i) == 1)
		{
			printf("%d\n", i);
		}
	}

	return 0;
}
