#include <stdio.h>
#include <string.h>
void Swap1(int* pa, int* pb)
{
	int temp = 0;
	temp = *pa;
	*pa = *pb;
	*pb = temp;
}
int main()
{
	int a = 10;
	int b = 20;
	Swap1(&a, &b);
	printf("a=%d n=%d\n",a,b);
	return 0;
}
